modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      follow_redirects: true
      valid_status_codes: []  # Defaults to 2xx
      method: GET
  icmp:
    prober: icmp
    timeout: 5s
    icmp:
      preferred_ip_protocol: "ip4"
